#include <stdio.h>

int main() {
    int n, arr[100], target, i, found = 0;
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &target);
 
    for (i = 0; i < n; i++) {
        if (arr[i] == target) {
            found = 1;
            break;
        }
    }
    if (found)
        printf("Found\n");
    else
        printf("Not Found\n");

    return 0;
}
